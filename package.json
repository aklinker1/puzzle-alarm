{
    "name": "puzzle-alarm",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "clean": "rm -f node_modules && yarn install",
        "format": "prettier --write .",
        "format:check": "prettier --check ."
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/aklinker1/puzzle-alarm.git"
    },
    "devDependencies": {
        "@prettier/plugin-xml": "^0.12.0",
        "prettier": "^2.1.2",
        "prettier-plugin-kotlin": "^2.0.0"
    }
}
